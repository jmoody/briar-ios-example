source 'https://rubygems.org'

gem 'faker', '~> 1.4'
gem 'xcpretty', '~> 0.1'
gem 'pry', '~> 0.10'
gem 'pry-nav'

# not yet
# gem 'twitter_cldr'

# use bundle and bundle config to specify local gem locations
# bundle config local.GEM_NAME /path/to/local/git/repository
gem 'run_loop', :github => 'git@github.com:calabash/run_loop', :branch => 'develop'
gem 'calabash-cucumber', :github => 'calabash/calabash-ios', :branch => 'develop'
gem 'briar', :github => 'jmoody/briar', :branch => 'develop'

# patch for ruby 2.2.0 and cucumber
gem 'minitest'

#gem 'xamarin-test-cloud', :git => 'git@github.com:jmoody/test-cloud-command-line.git', :branch => 'master'
