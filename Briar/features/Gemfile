source 'https://rubygems.org'

  if `whoami`.strip.eql? 'moody'

    # bundle config local.GEM_NAME /path/to/local/git/repository
    ### briar ###
    # gem 'briar', '0.0.9'
    # gem 'briar', :github => 'jmoody/briar', :branch => 'master'
    gem 'briar', :github => 'jmoody/briar', :branch => '0.0.9'

    ### calabash-cucumber ###
    # gem 'calabash-cucumber', :github => 'jmoody/calabash-ios', :branch => '0.9.x'
    # gem 'calabash-cucumber', :github => 'jmoody/calabash-ios', :branch => '0.9.x-riseup-branch'
    # gem 'calabash-cucumber', :github => 'jmoody/calabash-ios', :branch => '0.9.x-add-git-versioning-information'
    # gem 'calabash-cucumber', :github => 'driis/calabash-ios', :branch => '0.9.x-rotation'
    gem 'calabash-cucumber', :github => 'jmoody/calabash-ios', :branch => '0.9.x-extending-iOS7-rotation'


  else
    gem 'briar', '0.0.9'
    # briar uses 0.9.157
    # gem 'calabash-cucumber', '0.9.157'
  end




