source 'https://rubygems.org'


if `whoami`.strip.eql? 'moody'

  # i use bundle and bundle config to specify local gem locations
  # bundle config local.GEM_NAME /path/to/local/git/repository

  gem 'briar', :path => '~/git/briar', :branch => '0.1.3'
  gem 'calabash-cucumber', :path => '~/git/calabash-ios/calabash-cucumber', :branch => '0.9.x'

else
  gem 'briar', '0.1.3.rc1'
end

gem 'faker'

# nicht jetzt
# gem 'twitter_cldr'
